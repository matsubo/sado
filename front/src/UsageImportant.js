import React from 'react';

function UsageImportant() {
    return (
        <div role="alert" className="alert">
  <span class="material-symbols-outlined">
info
</span>
  <span className='text-sm'>タイムをクリックするとラップタイムが表示されます。名前をクリックすると行をハイライトします。カラム名をクリックするとソートされます。</span>
</div>

    );
}

export default UsageImportant;